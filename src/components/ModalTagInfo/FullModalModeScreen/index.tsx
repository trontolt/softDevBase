import React from 'react';
import { ITag } from '../../../interfaces/tags';
import './index.css';

export const FullModalModeScreen = ({ selectedTag }: { selectedTag: ITag }) => {
  return <div className="full-modal-screen">Short</div>;
};
